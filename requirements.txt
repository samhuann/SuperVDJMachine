# Loose runtime deps; see requirements.lock.txt for the exact versions the
# published results were produced with.
numpy>=1.20
pandas>=1.3
scipy>=1.7
tqdm>=4.0
olga>=1.3.0
sonnia>=0.4.0
openpyxl>=3.0
